import 'package:flutter/material.dart';

class BrowseScope extends StatefulWidget {
  final Widget child;
  const BrowseScope({super.key, required this.child});

  @override
  State<BrowseScope> createState() => _BrowseScopeState();
}

class _BrowseScopeState extends State<BrowseScope> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
